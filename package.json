{
  "name": "pipeline-visualizer",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "packages/adapters/*",
    "apps/*"
  ],
  "scripts": {
    "build": "tsc -b",
    "dev": "npm --workspace=@pipeline-visualizer/web run dev"
  },
  "devDependencies": {
    "@types/node": "^26.5.1",
    "typescript": "^5.4.5"
  }
}
